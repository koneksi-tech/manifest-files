apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: minio-api
  namespace: minio-devops
  annotations:
    meta.helm.sh/release-name: minio
    meta.helm.sh/release-namespace: minio-devops
spec:
  ingressClassName: traefik
  rules:
    - host: minio-api.seven-gen.net
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: minio-devops
                port:
                  number: 9000  # Update to the correct port
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: minio
  namespace: minio-devops
  annotations:
    meta.helm.sh/release-name: minio
    meta.helm.sh/release-namespace: minio-devops
spec:
  ingressClassName: traefik
  rules:
    - host: minio.seven-gen.net
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: minio-devops-console
                port:
                  number: 9001  # Update to the correct port
