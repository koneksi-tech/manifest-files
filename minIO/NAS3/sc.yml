apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: nfs-storage-class-nas3
provisioner: cluster.local/nfs-subdir-external-provisioner-nas3
parameters:
  archiveOnDelete: "false"
reclaimPolicy: Retain
mountOptions:
  - nfsvers=3
