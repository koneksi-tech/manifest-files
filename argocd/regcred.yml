# Legacy Docker Hub secret lived here as `dockerhub` (removed).
# NCR pull secret: edit placeholders in ncr-regcred.yaml and apply:
#   kubectl apply -f argocd/ncr-regcred.yaml
# Name `ncr-regcred` must match argocd/image-updater-values.yaml (pullsecret).
